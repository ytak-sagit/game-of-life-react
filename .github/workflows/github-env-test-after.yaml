name: GITHUB_ENV test after
on:
  workflow_run:
    workflows: Run Unit Test
    types: completed
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - run: echo "${TEST_RESULT}"
      - run: echo "${{ env.TEST_RESULT }}"
